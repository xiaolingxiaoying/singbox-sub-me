# GUI 重新设计

Status: claimed

## 需求

简洁、整齐、现代，围绕完整订阅导入和本机 sing-box 控制设计。保留 GPUI 和共享 client-core 控制面，保留订阅、节点、连接、日志、设置能力。

## 现状证据

- `crates/sbgui/src/main.rs`：概览同时在标题栏、页头、蓝色主卡和指标卡重复内核状态与操作。
- `dashboard_hero` 将 core_running 描述为网络就绪及保护连接，实际并不能证明网络接管或出口连通性。
- `network_path` 用连接数量表示公网出口，应避免把连接数量当作出口验证。
- 现有代码、DESIGN.md、client-core 和资源目录已有未提交修改，后续增量修改必须保留。

## 产品约束

使用 ClientSnapshot 作为显示依据，以 ClientCommand 执行操作。不得把演示节点、速率或连通性写入实际界面。内核、系统代理、TUN 配置与出口可用性分别表达。

## 设计方向

Operate 模式；浅色桌面网络控制台，中文系统字体，克制蓝色主操作，细边框、明确行对齐与标准设置控件。七个结构参照为：系统网络设置、连接管理器、文件管理器、桌面设备控制台、资源监视器、表格式网络诊断器、订阅资料库。种子 d45b199d 分配第四项桌面设备控制台；以可操作状态及层级为中心。

候选布局：A 连接优先；B 节点优先；C 状态与流量优先。生成设计预览后等待布局选择，再实现。图中数据为演示数据。

## 验收

- 首页有清晰主操作，状态不重复堆叠，文案不夸大网络能力。
- 六个页面保持统一间距、字体、控件、列表与空状态。
- 保持现有命令行为和用户修改。
- Rust 格式与 sbgui 编译检查，条件允许时验证原生窗口视觉和尺寸适配。


## 2026-09-17 方向修订与实现

用户否决第一轮大卡片预览，明确要求学习 Clash Party，优化优雅程度。以此作为具体方向继续实现，不沿用 A/B/C 旧图作为验收标准。

### 参考证据

- 官方截图：https://clashparty.org/docs/handson （已通过浏览器查看节点页图片）
- 官方组件结构：https://github.com/mihomo-party-org/clash-party/tree/smart_core/src/renderer/src/components/sider
- 借鉴常驻控制侧栏、模式分段按钮、紧凑网格和克制的标题层级；没有移植上游组件源码、商标或图片。

### 已实现

- 236px 控制侧栏，规则/全局/直连可直接选择。
- 系统代理与 TUN 模式常驻；TUN 在内核运行或启动中锁定，提示停核后切换。TUN 的选择状态不冒充运行状态。
- 6 个双列导航块，订阅摘要、实时上传下载与内核状态。
- 紧凑页头与唯一常驻内核操作；概览不再使用蓝色大横幅、重复四指标、未经检测的公网出口。
- 节点分组与网格；自动组不发送手动切换，点击测试停止事件传播。
- 订阅列表淡色选中，设置页分隔行。
- 内嵌 SVG 线性图标；GPUI 原生真实样本折线，无伪造速率、峰值或地理数据。
- 默认1080×760，最小860×640；连接表支持横向滚动。

### 验证与限制

- cargo check -p sbgui --offline 已通过。
- 独立审查确认三项必修修复：导航双列宽、标题栏按钮高度、次要文字对比度，均 resolved。
- Impeccable detector 返回 []；其 Rust 原生 UI 覆盖有限。
- 当前工具不支持原生窗口截图/操作，未完成运行视觉或点击验收；不将生成预览当实际截图。
- cargo build -p sbgui --release --offline 通过，产物复制到 dist/sbgui-clash-inspired.exe。
- cargo fmt -p sbgui --check 与 git diff --check 通过。
- 代码实现及构建完成，原生视觉验收仍待用户运行确认。

## 2026-09-19 原生运行视觉验收与引擎修复

补齐了此前"未完成运行视觉或点击验收"的欠账。桌面会话锁定导致合成鼠标输入不可用，改为给 sbgui
增加三个启动期评审接缝：`SBGUI_PAGE`（直接打开指定页面）、`SBGUI_SIZE`（覆盖窗口尺寸）、
`SBGUI_SHOW_EXIT_CONFIRM=1`（渲染退出确认弹窗而不启用系统代理）。配合 `PrintWindow` 截图脚本
（`.scratch/gui-screenshots/capture.ps1`、`shoot-all.sh`、`traffic-loop.sh`），七个页面 + 退出
弹窗 + 长页变体全部截取并逐一审查；演示数据（本地 sing-box 配置 + 官方内核 1.14.1 + 本地规则集）
在审查后已清理还原。

### 审查发现并修复的缺陷

1. **P0：tokio runtime 在 GPUI 启动闭包结束时被 drop**，引擎任务随 runtime 关闭被取消——GUI
   启动约一秒后所有异步操作（订阅缓存读取、流量轮询、命令）都以 "background task failed" 失败，
   界面停留在冻结快照。修复：runtime 有意泄漏为进程生命周期（`Box::leak`，附注释）。
2. **实时速率与内核内存恒为 0**：sing-box 的 `/traffic`、`/memory` 流式端点先推送一个零基线
   采样，而引擎每次轮询读取新流的第一个对象。修复：改为跳过基线读取第二个采样（流提前结束时
   回退到唯一采样）；TUI 一并受益。
3. **内核日志带 ANSI 颜色转义码**原样入库显示；引擎 tail 阶段现在剥离 CSI/OSC 序列。
4. **内核崩溃自动重启无上限**（如 9090 端口被残留内核占用时无限循环）；连续失败 5 次后停止并
   提示排查端口后手动启动。
5. `detect_version` 剥离 "sing-box version " 前缀，界面显示 `1.14.1`。
6. sbgui 界面细节：连接表包进与日志列表一致的白色面板容器；连接行无已解析 IP 时远程目标显示
   "-"（原为 ":80"）；本地导入的订阅卡片 URL 行显示说明文字（原为空白）；设置页激活档案行显示
   "使用中"（原仍显示"激活"按钮）；"延迟地址"输入框加宽到 220px。

### 验证

- `cargo test --workspace --offline`：client-core 32、sbtui 157、sbgui 全部通过；`tests/cli.rs`
  中 7 个 certbot/安装类失败在无本地改动的 stash 树上同样失败（预存环境依赖，与本工作无关）。
- `cargo fmt --check` 通过；`cargo build -p sbgui --release --offline` 通过。
- 运行视觉：全页截图见 `.scratch/gui-screenshots/01..09*.png`；实时速率、连接数、内核内存、
  日志、代理组、规则、退出确认弹窗均按预期渲染。
