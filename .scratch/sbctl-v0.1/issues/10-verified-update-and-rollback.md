# 10: 显式验证更新与回滚

**What to build:** 管理员可无副作用地检查更新，并在显式确认后完成经过验证、可健康检查且可回滚的 sbctl 与 sing-box 更新。

**Blocked by:** 09: 五协议安装与服务生命周期.

**Status:** ready-for-agent

- [ ] `update --check` 不下载、不替换、不重启任何服务。
- [ ] 显式更新仅接受固定发行清单中可验证的产物，并在切换前保留二进制、配置和状态回滚点。
- [ ] 校验、配置检查或健康检查失败会恢复可用旧版本；不会创建自动更新或每日重启任务。
