# 02: 持久配置与原子状态操作

**What to build:** 管理员可创建、读取和校验 sbctl 部署配置，并通过 `status` 看到持久化部署摘要；配置、生成物和状态的并发变更不会暴露半写入数据。

**Blocked by:** 01: 安全安装预检与 CLI 骨架.

**Status:** resolved

- [ ] 配置可表达订阅模式、主机、网卡、协议选择和受限秘密，且无效值被拒绝。
- [ ] 并发读取与写入只观察到旧的完整版本或新的完整版本。
- [ ] 写入失败或中断保留最后一个已知良好版本和受限文件权限。
