# 客户端视觉系统

`sbtui` 与 `sbgui` 共用状态颜色语义与控制面，但使用不同的布局与交互方式。界面优先回答三个问题：
内核是否运行、当前流量经由哪个节点、用户下一步可以安全执行什么操作。

## 共享控制面

两个 UI 都只渲染 `client-core` 发布的 `ClientSnapshot`，并只发送 `ClientCommand`。
订阅、内核、节点、系统代理、TUN、连接与日志的行为不属于界面，不得在 UI 内重复实现。

## 方向

- `sbgui` 使用中性灰白底色和白色内容表面；蓝色只用于当前选择与主操作。绿色表示正常、橙色表示需注意、红色表示失败。
- `sbgui` 采用紧凑桌面控制台的密度；主要容器圆角为 12px，小控件为 5–10px。标题、设置项名称、当前值和说明文字形成明确层级，不依赖装饰制造重点。
- `sbtui` 概览以语义网络路径为中心：本机 → 系统代理 → 当前节点 → 公网出口；`sbgui` 概览以运行提示、当前节点和真实流量趋势为中心。
- 不制造虚假的地理网络图；直连与代理流量通过明确文字说明，所有状态同时有颜色和文字。
- `sbtui` 在终端窗口低于 `100×28` 时切换为单列紧凑概览；`sbgui` 默认窗口为 1080×760px，最小为 860×640px，使用滚动区域与换行布局承载内容。

## sbtui 交互

- 键盘是主路径，页脚展示当前页的高频操作，`?` 提供完整快捷键帮助。
- 影响网络的操作必须可见并可恢复：模式切换、保留系统代理退出均使用二次确认浮层。
- 输入采用居中浮层，避免用户在复杂仪表盘中丢失焦点。

## sbtui 组件

- `draw_header`：运行状态、当前节点和实时速率的常驻摘要。
- `draw_dashboard`：全尺寸的路径仪表盘；`draw_dashboard_compact`：窄窗口降级布局。
- `draw_traffic_panel`：实时速率、近 5 分钟 sparkline、累计与峰值。
- `draw_help_overlay` / `draw_confirmation_overlay`：可发现性与高风险操作的安全层。

## sbgui

桌面客户端是一个专业、克制的网络控制台。实现仍为 GPUI 与 `client-core`；布局与交互以 `docs/gui-ui-redesign-recommendations.md` 和 `crates/sbgui/src/main.rs` 为准。

- 54px 自绘标题栏保留 Serein 品牌、原生拖拽区和窗口控制。左侧 224px 区域只承担七个页面的连续导航，底部仅保留实时上下行摘要，不再混放出站模式、代理、TUN、订阅和内核卡片。
- 页面标题区保持紧凑。其下的全局控制条在任意页面集中显示内核、出站模式、当前节点与延迟、系统代理和 TUN；启动可直接执行，停止和重启收进二级操作区。
- 主内容最大宽度为 1320px，左右内边距 24px，区块间距 14px。窗口最小尺寸为 860×640px；工具栏、控制条和指标区允许换行，表格允许横向滚动。
- 概览按「首次连接引导 / 连接状态 / 四个等权指标 / 流量趋势 / 运行状态」组织。流量图只绘制真实快照数据；1 小时和 24 小时在没有长期样本时显示为禁用状态。
- 订阅导入默认收进工具栏，点击后展开输入区；档案使用表格式行布局，以勾选、文字和淡青背景共同表达当前状态。
- 节点页默认使用紧凑列表，支持搜索、代理组切换、整组测速与卡片视图；当前节点有勾选和「当前」文字，延迟始终显示数值或明确的测试/超时状态。服务端名称里的 emoji 仅作为数据保留，界面展示时清理前缀。
- 规则页使用优先级、匹配条件、出站和状态四列表格；支持搜索，规则集默认折叠为摘要。
- 连接页优先显示应用/入口、远程目标、协议、命中规则、使用节点、累计流量和建立时间；支持搜索、暂停刷新、单条断开、详情面板和二次确认的全部关闭。`client-core` 同步解析 Clash API 的进程名与路径。
- 日志页使用常见的 `全部 / Debug / Info / Warn / Error` 精确级别筛选，正文使用等宽字体，并提供自动滚动、自动换行、复制、清空和导出。
- 设置页使用常规、网络与端口、内核、TUN、自动化、外观、高级七个标签；每次只显示一个单列设置组。文本设置统一由底部「保存更改」提交，并提示待保存及可能需要重启的更改。
- 退出确认使用「关闭系统代理并退出」作为安全主操作；「仅退出」明确提示系统代理会保留。
- 页面图标统一使用内嵌线性 SVG，不使用 emoji 充当功能图标。字体栈为 `Segoe UI Variable, Microsoft YaHei UI, Noto Sans SC, sans-serif`。
- 颜色固定为：背景 `#F7F8FA`、表面 `#FFFFFF`、主文字 `#101828`、次文字 `#667085`、边框 `#E4E7EC`、品牌 `#0F766E`、成功 `#15803D`、警告 `#B45309`、危险 `#DC2626`。
- 所有关键状态同时使用文字或图标表达，不依赖颜色；危险操作使用明确后果文案和确认步骤。

## 不变量

- 不改变订阅、内核、节点、系统代理、TUN、连接或日志的既有行为。
- 不能只用颜色或图标传达状态；每个关键状态必须有可读文本。
- `sbtui` 保持 Windows、Linux 与 macOS 终端的键盘兼容性。
