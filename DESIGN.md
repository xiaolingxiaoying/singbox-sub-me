# 客户端视觉系统

`sbtui` 与 `sbgui` 共用状态颜色语义与控制面，但使用不同的布局与交互方式。界面优先回答三个问题：
内核是否运行、当前流量经由哪个节点、用户下一步可以安全执行什么操作。

## 共享控制面

两个 UI 都只渲染 `client-core` 发布的 `ClientSnapshot`，并只发送 `ClientCommand`。
订阅、内核、节点、系统代理、TUN、连接与日志的行为不属于界面，不得在 UI 内重复实现。

## 方向

- `sbgui` 使用中性灰白底色和白色内容表面；蓝色只用于当前选择与主操作。绿色表示正常、橙色表示需注意、红色表示失败。
- `sbgui` 采用紧凑桌面控制台的密度；主要容器圆角为 12px，小控件为 5–10px。标题、设置项名称、当前值和说明文字形成明确层级，不依赖装饰制造重点。
- `sbtui` 概览以语义网络路径为中心：本机 → 系统代理 → 当前节点 → 公网出口；`sbgui` 概览以运行提示、当前节点和真实流量趋势为中心。
- 不制造虚假的地理网络图；直连与代理流量通过明确文字说明，所有状态同时有颜色和文字。
- `sbtui` 在终端窗口低于 `100×28` 时切换为单列紧凑概览；`sbgui` 默认窗口为 1080×760px，最小为 860×640px，使用滚动区域与换行布局承载内容。

## sbtui 交互

- 键盘是主路径，页脚展示当前页的高频操作，`?` 提供完整快捷键帮助。
- 影响网络的操作必须可见并可恢复：模式切换、保留系统代理退出均使用二次确认浮层。
- 输入采用居中浮层，避免用户在复杂仪表盘中丢失焦点。

## sbtui 组件

- `draw_header`：运行状态、当前节点和实时速率的常驻摘要。
- `draw_dashboard`：全尺寸的路径仪表盘；`draw_dashboard_compact`：窄窗口降级布局。
- `draw_traffic_panel`：实时速率、近 5 分钟 sparkline、累计与峰值。
- `draw_help_overlay` / `draw_confirmation_overlay`：可发现性与高风险操作的安全层。

## sbgui

桌面客户端以 Serein 原型的操作组织为视觉参考，采用紧凑控制侧栏与清晰工作区。实现仍为 GPUI 与 `client-core`，不引入上游源码。以下描述以 `crates/sbgui/src/main.rs` 为准，旧 `.impeccable/mocks` 图片不代表当前界面，也不作为运行验收证据。

- 左侧约 240px 控制侧栏使用参考 HTML 的纵向导航行，依次放置概览、订阅、节点、规则、连接、日志、设置七个页面入口；下方放置规则 / 全局 / 直连分段按钮、系统代理与 TUN 合并控制卡、当前订阅与用量摘要、上下行速率及内核状态。侧栏可纵向滚动。
- Windows 系统标题栏保持隐藏；标题栏中的 Serein 品牌、拖拽区及最小化 / 最大化 / 关闭控件均由 GPUI 绘制，不能恢复为系统标题栏或文本字符按钮。
- TUN 控制块在内核运行或启动中锁定，显示「停核后切换」；未锁定时以「已选择 / 未选择」表达配置状态。系统代理显示「已开启 / 已关闭」。
- 右侧页头使用 `SEREIN / PAGE` 路径、26px 页面标题、说明文字与启动 / 停止内核操作。概览使用可响应换行的四项实时指标、真实流量折线、当前节点与客户端事件双栏，不制造虚假的网络拓扑。
- 流量图通过 GPUI canvas 与路径绘制快照中的真实上下行采样，蓝色下载、绿色上传，附采样数、累计量与峰值；不使用字符画或虚构趋势。
- 设置页使用单列分组、标准设置行、右侧当前值与滑块开关，不使用仪表盘式的大色块表达普通配置。
- 订阅页支持从剪贴板导入 HTTP/HTTPS 地址、激活、刷新和删除档案；选中档案使用淡蓝底整行、蓝色边框与状态文字。
- 节点页使用可换行的紧凑网格，区分手动组与自动组；自动组由内核决定节点，不能手动切换。单节点测试阻止事件冒泡，不触发节点切换；超时节点显示红色「超时」。
- 规则页只读解析当前 `cache/active-config.json`，用序号、匹配条件、出站目标和启用状态表达路由顺序，不在 GUI 内复制一套配置编辑器。
- 连接页逐列展示状态、建立时间、类型、主机、规则、代理链、来源 IP、远程目标与累计上传/下载量，断开操作直接落在行内；表格最小宽度为 1040px，窄工作区允许横向滚动。
- 页面图标使用随程序内嵌的统一 SVG 资源；Serein 品牌图像与 Windows 可执行文件图标继续使用同源内嵌资源。页面字体为 Microsoft YaHei UI，页标题 19–26px，正文及控件通常为 12–14px，次级说明为 10–11px。
- 页面底色为 `#f5f6f7`，白色表面为 `#ffffff`，侧栏沉底表面为 `#f0f3f4`，分隔线为 `#dfe4e5`；主文字为 `#182022`，次级文字为 `#697475`。强调色为冷青 `#0a7374`，导航选中背景为淡青 `#e9f3f2`，普通选中表面为淡青 `#e7f3f2`；正常、注意、失败分别使用 `#237c5b`、`#9a640f`、`#c83e49`。
- 内容内边距为 24px，主要区块间距为 14px。依靠中性底色、白色表面、细分隔线及留白划分层级；强调态只在主操作、当前指标与选中导航中使用。
- 所有会改变网络的操作都必须有明确的文案与当前状态，不能只靠颜色表达。
- 终端快捷键、`?` 帮助、居中输入及二次确认浮层属于上方 `sbtui` 描述，不构成 GUI 已实现的交互承诺。当前记录来自源码核对，尚未完成原生窗口截图与视觉验收。

## 不变量

- 不改变订阅、内核、节点、系统代理、TUN、连接或日志的既有行为。
- 不能只用颜色或图标传达状态；每个关键状态必须有可读文本。
- `sbtui` 保持 Windows、Linux 与 macOS 终端的键盘兼容性。
